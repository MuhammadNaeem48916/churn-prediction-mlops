apiVersion: v1
kind: Service
metadata:
  name: churn-serving-svc
  namespace: ml-production
  labels:
    app: churn-serving
spec:
  selector:
    app: churn-serving
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: NodePort
